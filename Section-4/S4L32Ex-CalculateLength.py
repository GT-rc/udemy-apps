"""
Create a function that takes any string as argument and returns the length of that string.
"""

def str_length(a_string):
    return len(a_string)

print(str_length("The rain in Spain stays mainly in the plain."))
